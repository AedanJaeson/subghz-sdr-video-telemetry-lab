# Captures

Do not commit large IQ files to Git. Store large captures externally and commit only metadata, scripts, screenshots, or tiny synthetic examples when needed.

For every capture, record:

- date/time
- center frequency
- sample rate
- format
- gain
- antenna
- software/tool version
- reason for capture
- external storage location if applicable

